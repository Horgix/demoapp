sudo: required

services:
  - docker

before_install:
  - docker build -t horgix/demoapp .

script:
  - docker run -d horgix/demoapp

after_success:
    - if [ "$TRAVIS_BRANCH" == "master" ]; then
      docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
      docker push horgix/demoapp;
      fi
